# Go CLI i18n 规则

## 简介

该技能总结了本仓库 Go CLI 的多语言实现方式，包括语言检测优先级、翻译调用方式、Key 命名规范与双语文件维护流程。

## 适用场景

- 新增/修改 CLI 输出文案
- 增加新 skill 的描述文案
- 排查混合语言或缺失翻译问题

## 目录结构

```
go-i18n/
├── SKILL.md
└── LICENSE.txt
```

## 使用提示

更新 YAML 后需重新构建，且所有 key 必须在 `zh`/`en` 两个文件中同时存在。
